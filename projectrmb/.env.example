PORT=3000
# when running outside Docker use 127.0.0.1; docker-compose overrides this with host.docker.internal
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=project_rm
JWT_SECRET=change_me_secret
CORS_ORIGIN=http://localhost:5173
